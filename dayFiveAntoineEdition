with open("dayFive/values.txt") as f:
    lines = f.readlines()

omit = ["move", "from", "to", "\n", " "]
newlines = []
for line in lines:
    for word in omit:
        line = line.replace(word, '')
    newlines.append(line)

stack1 = ['RGJBTVZ']
stack2 = ['JRVL']
stack3 = ['SQF']
stack4 = ['ZHNLFVQG']
stack5 = ['RQTJCSMW']
stack6 = ['SWTCHF']
stack7 = ['DZCVFNJ']
stack8 = ['LGZDWRFQ']
stack9 = ['JBWVP']

crates = [stack1, stack2, stack3, stack4, stack5, stack6, stack7, stack8, stack9]
inst = line

for inst in newlines:
    print(inst)
    if len(inst) == 4:
        oldcolumn = int(inst[2])
        newcolumn = int(inst[3])
        numcrates = int(inst[0] + inst[1])
        # print(numcrates)
        # print(oldcolumn)
        # print(newcolumn)
    else:
        oldcolumn = int(inst[1])
        newcolumn = int(inst[2])
        numcrates = int(inst[0])
        # print(numcrates)
        # print(oldcolumn)
        # print(newcolumn)
        
    oldcolumn -= numcrates
    newcolumn += numcrates

    for stack in crates:
        
    
    # # Retrieve the crates from the old column
    # moved_crates = crates[oldcolumn - 1][-numcrates:]
    
    # # Remove the moved crates from the old column
    # crates[oldcolumn - 1] = crates[oldcolumn - 1][:-numcrates]

    # # Add the moved crates to the new column
    # crates[newcolumn - 1] += moved_crates

    print(crates)
